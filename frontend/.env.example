# Copy to .env for local dev, or set in your host's dashboard (Vercel/Netlify).
# Must start with REACT_APP_ — this is a Create React App (craco) project.

# The deployed backend origin (no trailing slash, no /api).
# Local dev:  http://localhost:8000
# Production: https://your-backend.onrender.com
REACT_APP_BACKEND_URL=http://localhost:8000
