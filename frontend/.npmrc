# React is 19.x but some transitive deps (e.g. @react-spring) still declare
# peer react ^16/17/18. Without this, npm on CI hosts (Vercel/Netlify) fails
# with ERESOLVE even though the app works fine.
legacy-peer-deps=true
