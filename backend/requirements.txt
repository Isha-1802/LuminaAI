# Minimal, deploy-clean dependency set — only what Lumina actually imports.
# The previous file had 131 packages (boto3, google-api-core, grpcio, stripe,
# huggingface, …) that the app never uses; their transitive constraints made
# pip fail with ResolutionImpossible on Render. This lists just the real deps.

# --- Web framework ---
fastapi==0.110.1
uvicorn==0.25.0
starlette==0.37.2
python-multipart==0.0.32      # FastAPI file uploads (UploadFile)
itsdangerous==2.2.0           # Starlette SessionMiddleware

# --- Database ---
motor==3.3.1
pymongo==4.6.3

# --- Data / validation ---
pydantic==2.13.4
email-validator==2.3.0        # pydantic EmailStr

# --- Auth ---
PyJWT==2.13.0
bcrypt==4.1.3
Authlib==1.7.2                # Google OAuth
httpx==0.28.1                 # Authlib transport

# --- AI ---
groq==1.5.0

# --- Files: resume parsing + PDF report ---
pypdf==6.14.2
python-docx==1.2.0
reportlab==4.2.5

# --- Config ---
python-dotenv==1.2.2
